const Dat = require('dat-node')
const Nanobus = require('nanobus')

class DatIdentity extends Nanobus {
  constructor (dir) {
    super()
    this.storage = null
    this.id = null
    this.
  }
}

module.exports = DatIdentity
